# 第1课-Excel快速入门

大家好，现在我们来学习第一课是要快速入门。这节课属于基础课程，我们将学习其中的基础操作和基本概念共分为三个部分，分别是入门的交界面以下基础操作和一些文件保护。

首先我们进入也是要见面的学习课程中使用的版本摄像二零幺六。如果你使用的四三二零幺三以上的版本，都可以完美的兼容课程中的所有的操作。

如果你使用的摄像二零幺零以下的版本或者是w p s 的同学，也不用太担心新的功能虽然不能向下兼容，但大部分的功能都还是有的。

不过院长还是建议大家升级到二零幺三版以上的版本，熟悉的小操作界面，是提高我们效率的基础。从零七百开始时使用了新的功能区代替原来。

菜单栏和工具栏。甚至我们可以根据个人习惯自定义操作界面。下面我们一起来认识一下砂的操作界面，看到最上方这里是标题了，显示的是当前打开的工作簿名称。如果我们看到这里显示的兼容模式，那么代表在这个工作簿里面是没有办法使用高级功能的。

我们怎么去分辨它是高版本还是低版本的，我们可以通过后缀名进行分辨。像现在显示的是h l s s 的话，那它就是一个高版本的工作部。

如果显示的是sls 的话，那它就是一个低版本的工作部。不知道在日常工作中，你们有没有遇到过这样的问题，自己使用的是高版本。

而身边的老板和同事们都还在使用低版本。这时候我们发过去的工作部，他们都没有办法打开，怎么办呢？我们每次都需要通过把文件。

成为低版本吗？不是的，我们可以通过点击文件选项卡，点击选项，点击保存。我们看到这里有一个选项叫要将文件保存为此格式。

这里我们选择可销。据悉，自二零零三工作簿点击确定设置完之后以后，我们在新建的工作簿进行保存。系统就会默认的把它当是每一个低版本的工作部了。再往下面看这个区域，我们称呼他为功能区，功能区里面的这些，我们称呼他为选项卡，而每个选项卡都会有相应的命令，他们是根据属性归类到不同的选项卡里面的。

例如说我们需要对单元的插入公式，它就忘记了是哪个公司的话，我们可以点击公式选项卡找到函数过。再根据类型和说明找到想要的东西。

中间这个区域是我们熟悉的工作表操作区域，我们可以在里面输入数据，一个工作簿，可以包含有很多个工作表。而低版本的工作部一个工作表只能存放六万五千五百三十六行的数据高版本形成一百多万了。

最下面这里我们称呼他为状态了。最左侧显示的是活动工作表，以及陆志虹的功能。右边这里是工作簿视图按钮，由普通视图页面布局视图和分页预览视图。不同的视图可以从不同的侧面预览我们的工作部。

一般我们默认选择普通视频最右侧我们可以通过缩放滑块来增大或者减少工作表的显示比例，也可以通过点击数字百分比选择说。

放比例进行调整，点击确定。这里重点讲一下显示比例里面的恰好容纳选定区域。我们在查找数据的时候，经常会被密密麻麻的数字感到头晕。这时候我们可以选中需要查看的区域，比如说选择这个区域，然后点击显示比例，选择恰好容纳选定区域点击确定。

我们会发现工作表就只会显示我们刚选择的数据区域啊，我们看回上方功能区，这里最左侧是文件选项卡。我们点击看一下，里面有很多我们熟悉的命令。

比如说新建由我们想要新建一个表格的话，直接点击空白工作簿就可以了。但是如果你觉得默认的模板已经不能够满足你的需求，在右边会有很多好看的一半。

例如我们点击基本销售五二表，无法点击创建，我们就可以直接新建使用对应的模板很重要。但在实际工作过程中，很多人都不知道他后面我们会专门讲如何创建模板即使用。

现在我们关闭他，如果这里没有你想要的模板，你也可以在搜索框里面输入关键字，点击搜索进行查找。必须往下面看，打开这里点击打开，我们会在右边看到很多最近打开的表格。

如果有常用的表格需要经常编辑。右边这里有一个小图町，点击他表格就会去到最上方的，以固定列表。这里他就不会随着表格的争夺，而对以其下来了。但如果我们需要把电脑借给别人，又不想让别人知道我们最近使用过的表格有没有办法嘛，这个列表全部清零呢？除了可以使用这种手动删除的方法以外，我们可以点击选项找到高级，我们把鼠标拖动到显示的这个位置，找到显示此树木的最近使用的工作部，我们把数字设置为零。

点击确定我们回到文件，打开最近打开的表格都都显示了，再看下面保存这里点击保存的话就会直接保存当前的两个。

那如果我们想要把表格保存在别的地方，又或者不想替换当前表格的话，我们可以选择另存为点击文件里面的映射问题。

点击浏览这里我们可以重新设置文件名提醒一下。如果对方使用的是幺零以下的版本，建议大家把保存类型。选择为九七至二零零三工作簿，这样就可以保证格式不会错乱的。好，这里先取消，除了如何保存，大家在使用内向的过程中，肯定会非常关心一个问题。

电脑死机了，但表格还没来得及保存怎么办？不用害怕，是可以自动的帮你保存文件，但前提是你要进行一定的设置，怎么操作呢？点击选项找到保存找到保存自动恢复信息时间间隔法勾勾上这里，建议大家把时间调整为五至十分钟，这里设置为五分钟。

这样子的话，即使我们没有来得及保存，也只是丢失了那几分钟的内容。再看这里，如果我没保存就关闭，请保留上次自动恢复的版本。

也要勾选上。如果我们在打开的时候，每上没有提示上一次自动赔付的版本，我们可以试一下。在这个路径里面找到他。其实大家有没有发现，很多时候我们遇到的小问题都可以在项选项里面进行设置。

它就相当于三个后台。如果你经常使用的消毒化，建议你一定要了解里面的这些选项。最上方的这个区域，我们称呼它为快速访问工具栏。这里重点介绍一下这么多的选项卡和按钮。

我们经常都会忘记要使用的命令在哪里。其实我们可以把经常使用的命令添加到快速访问工具栏。比如说我们需要把开始选项卡合并后居中车功能添加到快速访问工具栏。我们可以选中它点。

及鼠标右键点击添加到快速访问工具栏。然后他就躺到了最上端的这个区域里面了。那如果你想要找个功能，不在功能区。比如说我们想要在sr 里面增加一个计算器，我们可以在任意一个命令右键鼠标点击自定义快速访问工具栏，跳转到快速访问工具栏的选项框。

这里选择全部命令，他是按命令的拼音首字母排列的。计算器是j 开头的。我们重点讲一下j 开头的区域，找到计算器，选中它点击添加点击，确定计算器也添加到快速访问工具栏这里的。

但是如果每次都要移动到这里才能操作。的话感觉还是不够方便。这里有一个下拉按钮，点击他最下方，这里点击在功能区下方显示整个快速访问工具栏就跑到了工作表操作区域上方的这个位置了。

那这样操作起来就更方便了。除了可以自定义快速访问工具栏以外，我们也可以自定义功能区。比如说我们经常都要结合电子邮件使用，我们可以把它添加到开始选项卡里面，直接在上里面访问他跟自定义快速访问工具栏的方法非常相似。

我们可以在任意命令点击鼠标右键，点击自定义功能区。我们在常用命令这里长大。电子邮件我们可以直接把它添加到右侧的功能区吗？我们试一下点击添加。是不行的。

他会提示我们需要添加主我们添加命令到功能区，一定要先建筑才可以进行添加。现在我们在开始选项卡里面新建一个组，这个组命名为友兼并，即确定添加组之后再把电子邮件添加到里面，点击确定电子邮件就在开始选项卡里面啊，这样操作起来会不会更加方便呢？在讲当年的操作之前，我们首先要搞清楚的是里面的几个基本概念。

整一份表格。我们称呼大类工作部。工作簿里面有一个或者多个工作表，工作表里面用来存放数据的最小单元叫做单元格。单元格是有一个英文字母和一个阿拉伯数字组成的，由多个单元格组成的区域。

我们称呼他为大女人的区域。单元格中的英文字母，我们称呼他为列阿拉伯数字，我们称呼他为芽a 一单元格代表的就是第一列第一行的数据。

接下来我们学习单元一个的基本操作。前面提到单元格是有一个英文字母和一个阿拉伯数字组成的。比方说员工编号这个发明的他是a 一单元格，单元格里面可以存放不同类型格式的数据。比如说院长大大这是一个文本格式工作时间，这是一个日期格式。

公民失灵，这种是数字格式。那我们怎么去编辑大。多的内容啊。比如说我们现在要修改院长大大的员工编号，把它修改为一零。

我们点击鼠标左键选中它直接输入一零零按键盘的蝰蛇确定。但其实我们只需要修改单元格中的第三位，我们可以双击单元格进入编辑状态，把光标移动到第三位删除，重新修改为零。

但在日常工作中，由於单元格显示的长度和宽度不大，所以在操作的时候呢，如果没有对准单元的中间进行双击的话，我们看一下，如果鼠标变成这个形状进行双击，我们看到它就会跳到最下面的这个大严格了。

所以这里要介绍两种编辑单元格的方法。第一种我们可以选定单元格以后，按键盘的f 二键进入。编辑状态进行修改，这里重新修改为一。第二种，我们可以在编辑栏进行修改。

这里重新修改为例，这是对单元格内容的修改。我们看到在开始选项卡，这里除了修改单元格内容，我们还可以更改字体的大小，添加填充颜色，这些都可以通过字体这一个组的功能实现。

我们再看一下对齐方式。现在我们全部的数据都是居中对齐的。那如果我要修改对齐方式的话，选中单元格，点击向右对齐。我们就可以看到员工编号就会向右对齐了。

另外要讲一下合并后居中这个功能。为了表格的美化，很多小伙伴在使用你笑的时候。都会采用合并后居中。也就是我们经常说的合并单元格。

但这里的功能呢会对整个表格造成很大的影响。包括后续的数据排序筛选等，这些都是无法实现的。我们在后面的课程会详细介绍。另外对单元格的编辑。

还有一个大家很关心的问题就是制作表格。表头一般可以分为二级表头和三级表头。我们先制作一个二级表头，选中单元格，点击边框，点击其他边框点击这里点击确定。这就完成了一个二级表头的制作了他三级表格怎么制作呢？三十几表格的制作跟二级表格有点不一样。

我们需要通过点击插入选项卡，点击形状。选择第三个形状是一条直线。然后通过鼠标把这条直线画在当年的上面，再复制一条直线，这样就成功制作了一个三级表头了。我们现在来学习一下如何选择多个单元格选择多个单元格记忆单元的区域可以分为两种，一种是连续区域，像这样的就是连续区域，一种是不连续区域。

比如说我们要选择院长，然后巡视阿玛这几个就是一个不连续的区域。选择连续区域，我们有两种方法，第一种我们可以选择第一个单元格之后按着鼠标拖动，这样就可以选择一个连区区域的。

第二种我们可以点击b 二单元格院长大大，然后把鼠标移动到。这个单元格按着键盘的shift 键，然后再点击一下，也可以实现连续区域的选择。

那怎么选择不连续的区域呢？也有两种方法。第一种我们可以通过键盘的control 进行点选。比如说我们要选择院长搭档，然后下面这几个名称，我们可以按着键盘的恳求间进行一个个的点选。第二种我们可以激活添加选定模式，二人键盘的shift 加f。

这时候我们看到左下角这里出现了添加到所选内容这样的提示。那我们在选择的时候就不需要按着键盘的恳求剑呢直接点选就可以了。

选择完成后，只要我们按一下键盘的esc 键就退出了这个选定模式。但是我们一般很少这样去选择单元格，我们都是通过多行多列的单元格进行数据的。

去，那怎么操作呢？比如说我们要全选这一部分的数据，我们可以点击里面的任意一个单元格，按住键盘上的control 加a 我们发现整个区域都选择上了。

他有同学会问为什么在做恳求加a 的操作的时候，会把整个工作表都选择上呢？包括右边这些空白单元格，其实在做恳求加a 的时候，如果你选定的单元格是没有数据的。

比如说这里这时候如果你按键盘的恳求加a 就会把整个工作表示都选择上了。除了全选数据，有时候我们还需要选择某个区域的数据。

比如说我从员工编号x y 零幺零这里开始一直选择到数据表的最底部。那我们可以。通过拖拽鼠标来选择。但这样子的操作会非常慢，还可以通过什么方式实现呢？我们可以选中员工编号这个单元格按着建行的control 加shift 加右。

这时我们发现这个员工的整一行数据我们都选择上了，我们再按在键盘上的control 加shift 加下。

我们看到从零幺零这个编号的员工，到最后一个员工，所有的数据都已经选择上了。这里要知道的是，我们可以通过用control 加shift 加上下左右的方向键来进行数据的快速选取。

我们再举个例子，现在需要选择从院长大大到编号零幺零这个员工的数据怎么选择呢？是从院长大大开始往下进行选择嘛。

如果是从院长到。他开始的话，你往下选择的时候，就会把整个区域的数据都选择上了。所以我们的操作方式还是从零幺零这个编号开始，按着键盘的恳求加需加右选择这一行数据在按着control 加学府加上选择上面的数据。

此外，在查看工作表的时候，我们经常会查看到很后面的单元格。比如说这里这时候我们怎么快速的把鼠标定位。第一个大严格的，我们可以通过拖在右边的这个滑动条回到第一行第一列对吧，我们也可以通过按键盘的q q 将home 快速回到工作表的第一个单元格，学会选择单元格的单元格区域。

们来学习数据的复制与剪切。先讲数据的复制。如果我们要把这个区域的数据复制到一个新的位置，可以选中它按键盘的恳求加息，这时候处于复制的状态训中心的位置。

然后按一下键盘上的control 加v 把数据粘贴到新的位置，然后是数据的剪切。我要把这个区域的数据简洁到一个新的位置，我们可以选中它，按键盘的恳求加x 这时候选中一个新的位置，然后按一下键盘研究下，把数据粘贴到这个位置。

在讲完单元格的基本操作之后，我们来学习行列的操作，行以内的操作相似。我们你烈为例子进行讲解。首先是劣的选择。我们把鼠标移动到。

姓名这个列上面的英文字母b，点击鼠标左键选中心迷恋。如果要选中点区内的话，点击心理面，按住鼠标左键，然后往右拖，连续选择多练。

因为或者是选择性里面，然后按着shift 键点击一下岗位。好，也能够选择连续练。如果我们需要选择不连续的列的话，我们可以通过按键盘的两周键，然后点击姓名、岗位、学历、工作时间，这样就可以选择了几个露脸区的内容。

我们可以看到一个数据列的长度都是不同的。怎么对一个列的宽度进行调整呢？选择一列把鼠标移动到这个地方，显示为这个形状，然后拖动鼠标进行列宽的调整。

除了这种。动调整，我们还可以进行自动调整。比如说我们要对这些列进行自动调整，选中之后把鼠标移动到这里，显示为这个形状。双击鼠标左键他就会根据数据类的最长宽度进行自动调整。

除了鼠标的操作，我们还可以在开始选项卡，点击格式找到列宽自动调整列法对应的就是上述的两个操作。我们在查看数据的时候，经常会出现这样一种情况。

比如说我要查看哪位员工对应的工作时间。我们发现中间隔了很多其他的数据链，查看起来非常麻烦。其实我们可以把这些列隐藏起来，我们可以选中这几列，然后单击鼠标右键点击隐藏现在姓名。的后面就是工作时间。

我们在查看数据的时候就方便多了。但是这里要提醒一下大家，当你在查看数据以后，记得取消隐藏怎么操作呢？我们选中这两列，然后鼠标移动到中间，点击鼠标右键点击取消隐藏。

当你拿到别人的工作表，你不知道他有没有对行业进行隐藏的话，可以通过点击左上方的这个三角形全选工作表。然后在格式里面找到可见性，在隐藏和取消隐藏里面做两次操作，一次取消涵的操作。

你是取消列的操作就可以了。这时我们发现数据不够，需要进行补充。比如说我们要在身份证号后面增加出生日期。年龄的清晰，我们可以通过点击学历这一列，然后点击鼠标右键点击插入，这样就可以插入一个空白列了。

但这样插入列的话很慢，还有什么方法可以快速的插入练吗？第一种我们可以使用快捷键选中列之后，按键盘恳求shift 加等com 插入影片。

第二种选中一列把鼠标移动到这个位置，按着键盘shift 键鼠标变成这个形状后，往右拖动，也可以快速的增加。

恋怎么去删除力呢？我们可以通过选择这一列，然后点击鼠标右键点击删除，也可以通过快捷键的操作按键盘恳求加减号快速的删除一列。

最后我们呢。学习航旅业的复制与剪切。如果我们要把姓名这一列复制到一个新的地方，我们可以选中心里面。当键盘肯true 加息复制，然后在新的位置，在艾肯丘加v 把这一列的数据粘贴到新的位置。

另外我们也可以把数据列剪切到新的位置，选中性理念按键盘，恳求加s 剪切选中心的位置。在按键盘恳求加v 把它粘贴到一个新的位置。

但如果我们这样复制或者剪切的时候，会把原来列的数据给替换掉。如果我们不想替换怎么操作呢？比如说我们要把学弟加到失灵的前面，我们首先复制这一列，然后点击失灵，点击鼠标右键。

点击插入复制的单元格，这时他就不会替换原来的司令这一列了，而是在他前面增加了新的权利。练同样的剪切的时候，如果不想替换原来的面，也可以这样操作。

此外，再教大家一种快速调整猎的方法。比如说我们现在要调整身份证号的位置，可以通过按住键盘shift 键，然后把鼠标移动到这个位置，显示为这个形状。

我们按着鼠标然后往右拖动到公民的前面。最后我们来讲一下工作表的基本操作。我们在左下方这里可以看到当前活动的工作量，点击加号可以新建一张工作表。点击工作表名称可以切换工作表。

当一个工作簿有很多张。工作表的时候，我们可以把鼠标移动到这个位置，点击鼠标右键打开对话框，直接选中需要切换的工作表，点击确定。

除此以外，我们对工作表的基本操作还包括什么呢？我们可以选中一张工作表，点击鼠标右键可以看到有插入、删除、重命名等功能。

这里我们重点介绍一下移动或数字这个功能。我们点击它打开对话框。现在我们选定的是基础数据这张工作表我们可以把它移动到入门。稍见面前面去直接点击确定就可以了。

我们看到这里有一个建立副本，它其实就是复制的功能。我们勾选向他点击确定，这时候就会复制一份相同的工作表，放在了陆某的相界面前面的。

除此以外，我们看魁移动或复制，这里还有一个地方需要注意的。平时如果我们需要把一整张工作表复制到另外一个工作簿里面。

我们可以通过将选定工作表一致工作部这个功能来实现。比方说我们要把基础数据放在新的工作簿里面，这里点击星空桌布，点击建立副本，点击确定。

我们看到这时候新建了一张新的工作部，并且把这张工作表复制过来了。我们再讲一下选定全部工作表示的功能。使用这个功能，你可以对工作表进行批量操作。比如说我们现在要对全部工作表的a 一单元可进行修改。

我们点击选定全部工作表在a 一单元格插入一二三。一下回车。这时候全部工作表的a 一单元格都变成了一二三。我们具体看一下，最后我们简单介绍一下保护工作表。

如果我们不希望别人修改我们工作表的内容，点击保护工作表，输入密码一二三，点击确定再次输入。这时候别人就没有办法修改工作表的数据了。

信息安全在工作中是非常重要的。如果我们不想给别人随意打开工作簿的话，我们可以给工作部设置密码。我们点击文件选项卡，点击另存为点击浏览在保存那里的旁边。我们发现有一个工具按钮，点击工具点击常规选项，在打开权限密码这里设置密码一二三，点击确定，再次输入密码保存并替换原来的工作部。

那这个时候这份工作簿就是加密的，怎么看呢？我们可以在文件信息里面看到，这是一份需要密码才能打开的工作簿。

那如果现在我想把密码删除掉怎么办？我们回到另存为点击浏览在工具这里点击常规选项，把密码删除了，点击确定再次保存并替换。

那他现在就是一份没有加密的工作部了。除了通过设置密码不让别人。不随意打开我们的工作，不以外，我们也可以对工作簿的结构进行法库怎么操作呢？我们点击审阅选项卡，点击保护工作部署入密码一二三，点击确定再次输入。

这时我们会发现每个工作表都是能够正常编辑的。比如说对七十三这个单元格进行编辑是可以的。那么我们就没有办法去添加或者删除工作表，也没有办法诚命名或者移动等操作。如何解除保护呢？我们可以重新点击保护工作布设里输入密码即确定，现在就可以重新对工作簿的结构进行修改了。

刚才我们设置的是保护工作部，但别人还是能够对我们的工作表进行辨。寄的有没有办法，我们可以不让别人编辑我们的工作表呢？我们点击保护工作表，这里输入密码一二三，点击确定再次输入密码。

这个时候如果我们要编辑单元格的内容，比如说一十七这个来选择，他是没有办法编辑的。提示处于受保护的工作表中。

这个时候我们怎么取消工作表的保护呢？我们可以重新点击这个位置。现在这个功能修改为撤销工作表保护，点击输入密码。

现在我们重新对第一十期这个单元格进行编辑是可以的。细心的同学有没有发现我们刚刚在设置保护工作表的时候，这个位置有很多参数系统默认会把选定这两个参数勾选上。

如果我们在保护工作表的时候允许。方设置单元格格式，设置列格式和函格式的话，我们只需要勾选相应的参数，点击确定就可以了。

即使处在保护工作表的状态下，他也能够进行相应的操作。但我们要注意的一个地方是，即使你把全部的参数都空选项，你也是没有办法对单元格进行编辑的。

那我们发给别人的表格更多时候是希望对方纹路添加或者修改的区域的数据呢，那怎么操作呢？我们先点击取消，我们有没有发现右上角这里有一个允许用户编辑区域，我们点击他点击新建。

比如说我希望对方能够编辑t 五r w p 十五这个区域。这个时候我们可以通过修改引用单元格选择p 五。

大批食物这个区域点击确定，再次点击确定。然后我们在四设置保护工作表，输入密码一二三，点击确定。我们现在对第十七单元格进行编辑，是无法编辑着我们对p 五单元格进行编辑。可以正常编辑p 十一单元的，也是可以编辑的。

以上就是第一科的全部内容。我们下一次课再见。
